package com.magicvvu.fanzha.ui.theme

import androidx.compose.ui.graphics.Color

// New Brand Colors based on #0462C2 (Primary Blue)
val BrandPrimaryBlue = Color(0xFF0462C2)   // Main Theme Color
val BrandSecondaryBlue = Color(0xFFE6F0FA) // Light Blue for containers/backgrounds
val BrandNavy = Color(0xFF1E3A5F)          // Darker blue for text/headers
val BrandCoral = Color(0xFFED6160)         // Error/Alerts
val BrandOrange = Color(0xFFF29949)        // Warnings/Accents
val BrandTeal = Color(0xFF29BFC0)          // Success/Safe states

// Neutral Colors for Light Theme
val LightBackground = Color(0xFFF5F7FA)    // Very light cool grey/blue for background
val LightSurface = Color(0xFFFFFFFF)       // Pure white for cards
val LightTextPrimary = Color(0xFF1A202C)   // Near black for primary text
val LightTextSecondary = Color(0xFF64748B) // Slate grey for secondary text
val LightBorder = Color(0xFFE2E8F0)        // Light slate for borders

// Neutral Colors for Dark Theme (Optional, but defining for completeness if needed)
val DarkBackground = BrandNavy          // Navy as background for dark mode? Or a darker shade?
                                        // Let's stick to a clean Light Theme primarily as requested by "inputs hard to see" complaint.
